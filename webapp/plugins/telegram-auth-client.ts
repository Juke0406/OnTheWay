// We don't need a custom client plugin anymore since we're using the redirect approach
export const telegramAuthClient = () => {
  // This is just a placeholder function that returns an empty object
  // The actual Telegram login is handled by the Telegram widget and our server-side callback
  return {};
};
